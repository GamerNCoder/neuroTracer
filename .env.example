# Comma-separated web origins allowed to call the API (production).
# CORS_ALLOW_ORIGINS=https://your-app.vercel.app,http://localhost:3000

# Dev-only: batch-score files on disk
# NEUROTRACER_ALLOW_LOCAL_PATHS=1
# NEUROTRACER_LOCAL_PATH_ROOT=/absolute/path/to/data/human
